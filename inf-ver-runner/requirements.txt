httpx>=0.27.0
pydantic>=2.8.0
openai>=1.3.0
transformers==4.57.3
# CUDA torch, version-matched to the verifier vLLM image's torch
# (vllm/vllm-openai:v0.25.1 -> torch==2.11.0; the stack runs v0.25.1 with
# VLLM_USE_V2_MODEL_RUNNER=0 so the V1 sampler difr replays stays active):
# the Gumbel replay must draw noise from the same CUDA Philox RNG (and
# Philox offset stride) as the prover's vLLM. Bump in lockstep with
# ORCH_VLLM_IMAGE. See Dockerfile.
torch==2.11.0
